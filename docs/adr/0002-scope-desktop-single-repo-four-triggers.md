# 范围边界：桌面端、单仓库、四种触发

v1.0 只承诺桌面端（Windows/macOS/Linux），移动端明确不做——上游移动端是实验性的，依赖系统 git 与 Node 子进程，且不支持 SSH。同步范围是「一个 Vault = 一个 Git 仓库」，子模块多仓库留待后续。同步触发只有四种：手动、定时、启动时拉取、提交后推送；README 早期路线图中的「离开时同步」因 Obsidian 没有可靠的「离开仓库」事件而移除。

## Consequences

- 用户对移动端的期望被明确排除，发布说明与文档中需标注「仅桌面端」。
- 上游移动端相关代码在跟进时可不做重点适配。

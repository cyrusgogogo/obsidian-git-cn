# 建立 i18n 层而不是直接替换字符串

所有面向用户的文案通过轻量 i18n 模块渲染：源码保留英文 key，默认语言为中文，设置中提供「中文 / 英文」切换。上游没有 i18n 层；直接替换源码字符串虽然更简单，但会让每次跟进上游的合并冲突变大，也无法保留英文对照。选择 i18n 层是为了压低跟进成本，并遵守 AGENTS.md「翻译键用英文 key」的约定。

## Consequences

- 新增文案必须同时提供英文 key 与中文翻译。
- 汉化改动集中在翻译文件；跟进上游时的冲突集中在 key 引用处。
